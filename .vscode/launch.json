{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "bot",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/myfoodbot/",
            "args": [
                "-t", "BOT_TOKEN",
                "-d", "${workspaceFolder}/myfood.db"
            ]
        },
    ]
}